version: "3.9"
services:
  managerx:
    build: .
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production

  nginx:
    image: nginx:stable-alpine
    depends_on:
      - managerx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
